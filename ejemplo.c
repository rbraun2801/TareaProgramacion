#include <stdio.h>
int main()
{
    /* code */
    char articulos[5]={'hola','v','g','l','L'};
    int precios[5]={1,2,3,4,5};
    for (size_t i = 0; i < 5; i++)
    {
        if(precios[i]==1);
            printf(" %c %d\n",articulos[i], precios[i]);
        /* code */
    }
    scanf("%c",&articulos[2]);
     for (size_t i = 0; i < 5; i++)
    {
        if(precios[i]==1);
            printf(" %c %d\n",articulos[i], precios[i]);
        /* code */
    }
    return 0;
}
